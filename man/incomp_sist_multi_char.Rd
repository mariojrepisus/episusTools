% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incomp_sist_mult_char.R
\name{incomp_sist_multi_char}
\alias{incomp_sist_multi_char}
\title{Avaliação de Completude de Variáveis (Não Datas)}
\usage{
incomp_sist_multi_char(
  banco,
  variavel,
  ignorado,
  nulo = NA,
  nivel_validos = 90,
  nivel_ign = 5
)
}
\arguments{
\item{banco}{Data frame com os dados.}

\item{variavel}{Vetor de nomes (strings) das variáveis a serem avaliadas (exceto aquelas
que começam com "DT").}

\item{ignorado}{Vetor de valores considerados ignorados.}

\item{nulo}{Valor (ou vetor) considerado nulo (padrão: \code{NA}).}

\item{nivel_validos}{Percentual mínimo de observações válidas para avaliar como adequado (padrão 90).}

\item{nivel_ign}{Percentual máximo de observações ignoradas permitido (padrão 5).}
}
\value{
Um \code{tibble} com a avaliação de completude para cada variável.
}
\description{
Avalia a completude de variáveis não referentes a datas em um data frame,
informando percentuais de observações válidas, nulas e ignoradas.
}
\examples{
\dontrun{
  resultados <- incomp_sist_multi_char(banco, c("var1", "var2"), ignorado = c("NA", ""))
}
}
